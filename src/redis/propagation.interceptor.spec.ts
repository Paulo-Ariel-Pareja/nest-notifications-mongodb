import { PropagationInterceptor } from './propagation.interceptor';

describe('PropagationInterceptor', () => {
  it('should be defined', () => {
    expect(new PropagationInterceptor()).toBeDefined();
  });
});
